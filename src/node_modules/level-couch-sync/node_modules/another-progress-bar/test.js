var bar = require('./')()

var i = 0
var int = setInterval(function () {
  bar.label(new Date())
  bar.progress(++i, 352)

  //go one tick longer, 
  if(i > 352) {
    clearInterval(int)
    //don't exit immediately, so user sees 100%
    setTimeout(function () {
    } , 1000)
  }
}, 20)

